spring:
  application:
    name: spring-data

  datasource:
    url: jdbc:postgresql://localhost:5432/books
    driver-class-name: org.postgresql.Driver
    username: postgres
    password: root

  jpa:
    generate-ddl: true
    show-sql: true
    hibernate:
      ddl-auto: create
